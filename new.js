const age = 16
const text = (age <= 18) ? "Minor" : "Adult";
document.getElementById ("demo").innerHTML = text;

// const age = 16;
// const text = (age <= 18) ? "Minor" : "Adult";

// document.getElementById("demo").innerHTML = text;